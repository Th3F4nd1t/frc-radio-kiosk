# This directory holds the persistent configuration file (config.json).
# The file is created automatically on first run and should NOT be committed
# to version control so that different deployments keep their own settings.
